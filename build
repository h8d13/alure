#!/bin/bash
set -euo pipefail

# Clean the cache to ensure fresh download
rm -f /var/cache/distfiles/alure-master.tar.gz

# Fetch sources
abuild fetch

# Update checksums
abuild checksum

# Build and package
abuild -r
